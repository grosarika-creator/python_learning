# Session 3: String Operations & Text Processing

- **Date**: 2026-09-07
- **Topic**: Strings, text transformation, and basic string methods

## Overview

In this session, I learned how Python handles text through string operations. I practiced changing the case of text, replacing words, and checking whether a sentence contains certain patterns. I also saw how small text-processing examples can help me understand the usefulness of built-in string methods in everyday programming.

---

## Files and Explanations

### `[string_operation.py](./string_operation.py)`

- **Concept**: String methods and transformations
- **Explanation**:
  In this file, I used built-in methods such as `lower()`, `capitalize()`, `title()`, and `replace()`. These methods let me manipulate text without rewriting the whole string manually. I also explored how a sentence can be normalized before comparison or checking, which is a common practical pattern in Python programs.
- **Possible Improvements**:
  - Add comments to explain what each method does.
  - Try combining multiple methods in one example to show the order of operations clearly.
  - Compare the result of `lower()`, `upper()`, and `title()` to better understand their differences.

---

## Key Takeaways

- I learned that Python strings are flexible and can be transformed in many ways.
- I practiced using methods like `lower()`, `capitalize()`, `title()`, and `replace()`.
- I understood that text processing is important when handling user input or comparing strings.
- I started to see how simple string operations can be used in more realistic applications.
